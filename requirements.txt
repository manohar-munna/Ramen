# Core PDF Engine & Geometry Parser
pymupdf>=1.23.0

# Computer Vision & Image Processing
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=10.0.0
scikit-image>=0.22.0

# Document Layout Detection, OCR & Table Recognition
paddlepaddle>=3.0.0
paddleocr>=3.0.0
paddlex>=3.7.0

# Backend Desktop API Server
fastapi>=0.110.0
uvicorn>=0.28.0
python-multipart>=0.0.9
pydantic>=2.5.0

# Testing. httpx is what fastapi's TestClient is built on, so the suite does not
# import without it.
pytest>=8.0.0
httpx>=0.27.0
