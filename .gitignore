# Models and large binaries
models/
cache/
outputs/
temp/
*.onnx
*.pdmodel
*.pdiparams
*.pdopt
*.bin
*.tar
*.zip

# Python
__pycache__/
*.py[cod]
*.class
*.so
.env
venv/
env/
.venv/
build/
dist/

# OS files
.DS_Store
Thumbs.db

# Scratch artifacts
scratch/

# Training data, training runs, and base detector weights. datasets/ is fetched by
# mlkit/fetch_dataset.py, weights/*.pt are downloaded by ultralytics on first run,
# and models/ holds what training produced. All reproducible, none of it the app.
datasets/
weights/
runs/
*.pt
